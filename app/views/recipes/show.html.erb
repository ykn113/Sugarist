<div class="container mt-5">
  <div class="row">
    <div class="recipe-headding mb-3 ml-3">
      <h1><%= @recipe.name %></h1>
    </div>
  </div>

  <div class="row mb-5">
    <div class="col-md-5">

      <%= link_to recipe_path(@recipe.id) do %>
        <%= attachment_image_tag @recipe,:recipe_image, size:"400x550",  fallback: "no_image.jpg" %>
      <% end %>
    </div>

    <div class="col-md-3">
      <div class="recipe-ingredients">
        <h3>Ingredients</h3>
          <%= sanitize @recipe.ingredient, tags:%w(strong em p br div ul ol li) %>
      </div>
    </div>

    <div class="col-md-4">
      <div class="recipe-details">

        <div class="recipe-detail-serves">
          <span class="detail">
            <%= @recipe.serve%>人分
          </span>
        </div>

        <div class="recipe-detail-serves">
          <span class="detail">
            調理時間
          </span>
          <%= @recipe.cooking_time %>
        </div>

        <div class="recipe-detail-serves">
          <span class="detail">
            難易度
          </span>
          <!--ここに難易度の星追加-->
        </div>

        <div class="tag-list">
          <h6>Tags</h6>
            <% @recipe.recipe_tags.each do |recipe_tag| %>
              <%= link_to recipe_tag.tag.tag_name, recipes_path(tag_id: recipe_tag.tag.id) %>
            <% end %>
        </div>

        <div class="memo">
          <h4>Memo</h4>
            <%= sanitize @recipe.memo, tags:%w(strong em p br div ul ol li) %>
        </div>

        <div class="views">
          <h4>Views</h4>
            <%= @recipe.impressionist_count %>
        </div>
      </div>


        <%= link_to "Edit", edit_recipe_path, class:"btn btn-sm btn-primary" %></td>
        <%= link_to "Destroy", recipe_path(@recipe),method: :delete, class:"btn btn-sm btn-primary", "data-confirm" =>"本当に消しますか?" %>
      </div>
    </div>

    <div class="mt-3">
      <div class="method mb-3">
        <h3>Method</h3>
          <%= sanitize @recipe.method, tags:%w(strong em p br div ul ol li) %>
      </div>
    </div>

  </div>
</div>